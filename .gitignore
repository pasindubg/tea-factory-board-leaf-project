node_modules/
dist/
.next/
.expo/
.turbo/
.env
.env.local
.env.hosted
.DS_Store
*.log
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
# Local design/QA screenshots and scratch output. Ignore the whole directory:
# the previous `.artifacts/*.png` matched only PNGs sitting directly inside it,
# so any other format or nested file still got committed.
.artifacts/
*.chagptcode
*.chatgptcode

# TypeScript incremental build cache. Regenerated by every `tsc` run, so a
# tracked copy differs on every branch and conflicts on every merge.
*.tsbuildinfo

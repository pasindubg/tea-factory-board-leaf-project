; Dependency contract: static = database FK/lifecycle dependency.
; dynamic = runtime read/integration without entity ownership.
; Every table is declared by exactly one module's entities.sql.
suppliers=dynamic
weighings=dynamic

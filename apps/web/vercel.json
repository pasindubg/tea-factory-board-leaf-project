{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "set -e; if [ \"$VERCEL_ENV\" = \"production\" ]; then echo \"Production build — applying pending Drizzle migrations against ${DATABASE_URL##*@}\"; pnpm --filter @tea/db db:migrate; fi; pnpm exec turbo run build --filter=web"
}
